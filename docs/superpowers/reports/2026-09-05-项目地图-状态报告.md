# legado（Arisemoss/legado）· 项目地图与状态报告

> **调研基线**：`eec4139cd`（master，2026-08-24 17:46，versionCode 逻辑值 5771）
> **报告日期**：2026-09-05　**性质**：总控流程 阶段1~2 全项目调研产物（五路并行深读 + 主控独立复核，全程只读、未跑构建）
> **证据规则**：每条结论附 代码路径:行号 / git 命令结果 / 文档出处；无法核实处标注「待确认」，禁止编造。
> **刷新钩子**：本文档一切以 HEAD=上方 commit 为真值；后续任何代码改动后请更新本报告（§10 Checklist 提供例行核对项）。

---

## 0. TL;DR（一页结论）

| 维度 | 结论 |
|------|------|
| 项目本质 | gedoor/legado「阅读3.0」2020-08-31 快照（fork 点 401581f61）之上的 **AI Agent 深度改造 fork**；原版阅读能力全保留，叠加 ai/ 平台层（44 文件 / 5021 行） |
| 当前形态 | 单模块（settings.gradle 仅 :app）、纯 View 体系：Compose 0 / WorkManager 0 / DataBinding 0；kotlin-android-extensions synthetic 172 文件 + Anko |
| 代码健康度 | master 干净、CI 四条 workflow 在列；fork 增量 86 提交集中在 08-22→08-24（含 10 次 2020 上游 merge，非新内容）；近端 P0/P1 修复到位、4 组 AI 单测已入 |
| 文档成熟度 | 13 份文档（docs/ 10 md + README + api.md + CHANGELOG）均在库、无断链；但统计口径普遍过期（§6.1 漂移表 D1-D22） |
| 最大风险 | S1 签名私钥+口令明文入库（供应链信任）· S2 readerProvider exported 无鉴权 · S3 Web 服务无鉴权+CORS 反射 · R1 上游基线陈旧 rebase 成本极高 · R5 文档滞后节奏 |
| 最优先下一步 | 1) 本文档入库+README 链接（即本次交付）；2) P0 专项：签名迁移至 Secrets/独立 keystore、ReaderProvider 收紧、Web 服务 token；3) 数据面：主线程 DB 访问收敛评估；4) 文档例行核对机制 |

## 1. Background（背景与定位）

- fork 谱系：gedoor/legado（source）→ Carl-Wen/legado（parent）→ Arisemoss/legado（本仓，创建 2026-08-11）。上游快照基线 = fork 点 401581f61（2020-08-31 22:00 +0800，gedoor 提交，merge-base 自证为 HEAD 祖先）。
- fork 增量实测（401581f61..HEAD）：86 提交 / 163 文件 / +11212/-128；其中 76 个非 merge、10 个 2020 上游 merge（无新内容）、--first-parent 82。ai/ 路径文件 44。
- 定位：原版能力（网络书源/本地书籍/书架/朗读/RSS/Web 服务/ContentProvider API）完整保留；新增 **AI Agent 平台**：对话助手 + 22 工具 + auto/native/text 三协议 + 写操作 pending_confirm 二次确认 + 应用内运行日志 + 现代阅读配色与毛玻璃 UI。
- 演进分期（commit 时间线）：
  1. **基座期**（08-22）：移除停服 Fabric/Crashlytics（25de2f086）、CI JDK/SDK 次序修复（2f407c46c）、debug 签名包（5606e0172）。
  2. **AI 核心期**（08-22~08-23）：真·流式 SSE+工具链路提速（8dbde84e7）、配置逐字段自愈（b46974a15）、应用内日志页（45d7ca20e）、RikkaHub 设计语言移植（a4de4e6a9）、Operit 文本 XML 协议（300bb8eac）、全量 UI 审核修复（1e1abfe4c）。
  3. **环境化期**（08-23~08-24）：悬浮球/后台任务中心/思考折叠（8db11787c）、M3 主题令牌资源（57b76f395）、P0 三缺陷（26e64265e）、P1 技术债（ed991c457：协议降级/ApprovalBus 确认总线/搜索随机化/窗口取反修复）。
  4. **收尾期**（08-24）：AI 单测 4 组+桥 scope 泄漏修复（ebbfaec6a）、coroutines 1.3.7 CI 兼容（3e2364e62）、五套 MIT 配色（0eb8eea85，仅 assets+README）、毛玻璃质感（eec4139cd=HEAD）。
  5. **发布滞后事实**：仓内正式包 ai-apk-download/legado-ai-apk/legado_ai_3.26.082315-ai-v2.apk（18,049,343B）构建于 commit 3f3541d3c 的父提交（rev-list 5758，包标注 5757），落后 HEAD 14 提交；当前应以 Actions artifact 为准。
## 2. Current State（仓库现状）

### 2.1 版本与构建身份
- 包名（buildTypes suffix 叠加 flavor）：`io.legado.app.release`（原版）/ `io.legado.app.debug` / `io.legado.ai.release|debug`（AI 共存主线）/ `io.legado.play.*`（google flavor，`io.legado.play` + suffix）。与 README「包名说明」一致。
- versionName = `"3." + yy.MMddHH(GMT+8) + "-ai-v2"`（app/build.gradle:11-16，`releaseTime()` 格式化）；versionCode = `git rev-list --count HEAD`（app/build.gradle:17，整库历史计数，HEAD=5771）。
- SDK：compileSdk 29 / minSdk 21 / targetSdk 29（app/build.gradle:20,36-37）；工具链 Kotlin 1.4.0 / AGP 4.0.1 / Gradle wrapper 6.1.1；mise.toml 钉 JDK 11.0.2。
- 签名：release/debug 均仅在 `RELEASE_STORE_FILE` 属性存在时签名（app/build.gradle:22-33,55,63）；CI 中复制 `.github/workflows/legado.jks` 且**口令明文**（见 §5.1 S1）。
- 跟踪产物面：`app/` 1018 文件（另有 14 个中文路径）；`ai-apk-download/` 2（正式包 18MB）；`build-artifacts/` 2；`.trae-html-share-packages/` 3；`.github/` 5（4 workflows + legado.jks）；`.gitignore` 未覆盖 `*.jks` 与上述产物目录。

### 2.2 顶层结构图
```
legado/
├── app/                         # 唯一 Gradle 模块（:app）
│   ├── src/main/                #   io.legado.app.* 全量代码（513 kt / 62,159 行）
│   ├── src/ai/                  #   AI 共存 flavor 专属资源/清单
│   ├── src/google/  src/debug/
│   ├── src/test/                #   AI 纯逻辑单测（4 组）+ Example 桩
│   └── src/androidTest/
├── docs/                        # 见 §6 文档清单
├── .github/workflows/           # ai-build / android / legado / autoupdatefork + legado.jks
├── ai-apk-download/             # 仓库内正式安装包（滞后 HEAD 14 提交）
├── api.md                       # Web/ContentProvider API 手册（上游体例）
├── build-artifacts/  .trae-html-share-packages/
├── README.md  LICENSE  CHANGELOG.md(上游遗留 1.0.0)  package.json(上游遗留,URL 指 gedoor)
└── gradle.properties  settings.gradle  build.gradle  mise.toml
```

### 2.3 包级地图（io.legado.app）
```
app/ (Kotlin 主包，513 文件/62159 行, 实测)         fork 增量文件分布（163 改动文件）
├── ai/            AI 平台层 44 文件/5021 行       ├── ai/** ~49 文件（含新增）
│   ├── runtime/   AgentRuntime·ApprovalBus·ToolExecutor·OpenAIClient·SSE·AiKeyStore·
│   │              ChatModelClient·ConversationService·AgentTaskCenter·SystemPromptBuilder
│   ├── tool/      ToolRegistry·TextToolCallParser·7 实现文件(22 工具, ToolRegistryFactory)
│   ├── bridge/    AiBridge + 5 Default* 桥（读/写分离）
│   ├── skill/     SkillRegistry 领域技能
│   ├── model/     AiModelConfig·AiProviderPresets(13 家)·ChatMessage·ToolDefinition·ToolEvent·AgentError
│   ├── log/       AiLog（环形+文件，含 mask 脱敏）
│   ├── ui/        AgentHubActivity·AgentHubViewModel(StateFlow)·AiLogActivity·AIFloatBallView·AuroraBackgroundView·GlassEffect
│   ├── ModelManager.kt · AiPlatform.kt（装配点）
├── base/          基类 VMBaseActivity/Fragment · ui 复用
├── constant/      PreferKey(ai_* 键 14 个:66-85)·EventBus·AppConst
├── data/          Room: AppDatabase(v20, 18 @Entity, 迁移 10→20) · 17 DAO · entities/22 文件
│   └── migrate/   Migration19To20.kt(独立文件) + AppDatabase 内联迁移
├── help/          AppConfig·CrashHandler·ActivityHelp·ReadBookConfig·http/AjaxWebView 等
├── lib/           theme(ATH/ThemeStore)·LiveEventBus 等库封装
├── model/         analyzeRule(规则引擎 6 文件)·webBook·localBook·rss
├── receiver/      MediaButtonReceiver·SharedReceiverActivity·TimeBatteryReceiver
├── service/       6 服务（AudioPlay/BaseReadAloud/CheckSource/Download/HttpReadAloud/TTSReadAloud）+ WebService + help/
├── ui/            13 顶层子包：about association audio book config filechooser login main qrcode
│                  replacerule rss welcome widget（书城/书架/阅读在 book/ 下）
├── utils/         工具 35 文件（含 ViewModelKt 等）
├── web/           HttpServer(NanoHTTPD)·WebSocketServer·controller/{Bookshelf,Source,SourceDebugWebSocket}
├── api/           ReaderProvider(ContentProvider 端点, exported=true) 等
└── App.kt         启动装配（见 §4.1）
```
## 3. Architecture（分层架构总览）

```
┌───────────────────────────────────────────────────────────────┐
│ UI 层：纯 View 体系（无 Compose/DataBinding/ViewBinding）          │
│   ui/main MainActivity(kt:32-46) 四页签：书架/发现(书城)/订阅/我的  │
│   ui/book/read 阅读引擎(ReadBookActivity + page/* 翻页)           │
│   AI UI: ai/ui/AgentHub(+VM,StateFlow)·AiLogActivity·AIFloatBall· │
│          AuroraBackgroundView·GlassEffect（共6文件/1532行）       │
│   绑定方式：kotlinx.android.synthetic ×172 文件 + Anko           │
├───────────────────────────────────────────────────────────────┤
│ AI 平台层 io.legado.app.ai（44 文件 / 5021 行）                   │
│   装配: AiPlatform.init→5 Default桥→ToolRegistry(22)→syncConfig  │
│   runtime: AgentRuntime(多轮) OpenAIClient(SSE) ToolExecutor      │
│            ApprovalBus AgentTaskCenter ConversationService        │
│            AiKeyStore SystemPromptBuilder ChatModelClient         │
│   bridge: AiBridge(5接口聚合: 3读1读写1只写强制确认)                 │
│   tool: ToolRegistry TextToolCallParser impl/7文件(22工具)        │
│   skill/model/log/ui 见 §3.1                                       │
├───────────────────────────────────────────────────────────────┤
│ 业务核心层（上游 2020-08 基座, fork 侵入有限 — 见 §3.2）            │
│   model/analyzeRule(6文件: AnalyzeRule 646行/AnalyzeUrl 455行等)  │
│   model/{webBook,localBook,rss} · help/(config,book,storage,http) │
│   service/×6+WebService · web/(NanoHTTPD 9路由+WS 1123)           │
│   api/(ReaderProvider: uriMatcher 9路径, exported=true)           │
├───────────────────────────────────────────────────────────────┤
│ 数据层 Room v20（17 DAO × 18 @Entity；entities/ 22 文件）          │
│   迁移链 10→20（AppDatabase.kt 内联 + migrate/Migration19To20.kt）│
│   ⚠ fallbackToDestructiveMigration + allowMainThreadQueries       │
└───────────────────────────────────────────────────────────────┘
```

### 3.1 AI 平台层要素表（证据 path:line）

| 要素 | 现状 | 出处 |
|------|------|------|
| 装配 | App.onCreate: AiLog.attach(:67)→AiPlatform.init(:69)；init 构造 5 Default 桥→buildRegistry→syncConfig | App.kt / AiPlatform.kt:47-58 |
| 热更新 | syncConfig 配置变化才重建 client+runtime；防御式读取坏配置沿用旧值；AgentHub onResume 触发 | AiPlatform.kt / AgentHubViewModel.kt:340 |
| 多轮 | repeat(maxRounds)，默认 5（构造默认+pref 默认均 5）；stopRequested→STOPPED 保留已产出 | AgentRuntime.kt:39,105-108 / ModelManager.kt:72 |
| 历史窗口 | HISTORY_CAP=24（systemPrompt+history.takeLast(24)+userPrompt） | AgentRuntime.kt:56,94-97 |
| 预算 | 装配硬编码 16_000（AiPlatform.kt:86-87；文档 :83 行号漂移）；runtime 构造默认 32_000(:40)被覆盖；usage 缺失按 256+len/3 估；超限 BUDGET_EXCEEDED 截断 | AgentRuntime.kt:136-146 |
| SSE | 流式分块 content/tool_calls/usage；[DONE] 断流；非 SSE 响应自动回退 parseCompletion；非 2xx 映射 AUTH_FAILED | OpenAIClient.kt:76-171,152-155 |
| 请求超时 | OpenAIClient 默认 120_000ms（:28）；OkHttp connect/write/read 同值 | OpenAIClient.kt:34-38 |
| 确认窗口 | ApprovalBus.await(token, confirmTimeoutMs)；默认 300_000(:42)，装配 maxOf(timeout,300_000)(AiPlatform.kt:88)→**实际≥300s**；文档 120s 口径过期（120s 是请求超时，机制被混写） | AgentRuntime.kt:303-305 / AiPlatform.kt:88 |
| 确认总线 | 单槽 decision + 100ms 轮询（1.3.x 无 SharedFlow 订阅语义，注释自陈）；token 一次性消费；不匹配丢弃；UI 入口 VM.approve→runtime.approve | ApprovalBus.kt:9-43 / AgentHubViewModel.kt:320-328 |
| 三协议 | auto(默认)/native/text：text 不发原生 schema+XML 清单注入；XML 解析仅当无原生 calls 且非 native | AgentRuntime.kt:126,258-262 / ChatMessage.kt:96-102 |
| XML 解析 | 正则 tool/param + CDATA/实体还原 + 代码围栏剥离；id=txt_ts_seq；快速路径零开销；有单测 | TextToolCallParser.kt / Test |
| Key 存储 | AndroidKeyStore AES/GCM/NoPadding 别名 legado_ai_key；密文存 ai_api_key_enc；降级：SDK<23 明文、解密失败回退明文（告警）、put 主密钥不可用明文；读时发现明文自动迁移 | AiKeyStore.kt:27-82（P0-1 自愈） |
| 日志 | 内存环形 800 + filesDir/logs/ai.log(512KB 轮转) + logcat；**不落完整请求体**（摘要 take80/args200/结果160-240）；Key 全 mask（首尾4字符） | AiLog.kt / AgentRuntime.kt:100 等 |
| 工具 | 22 个与 README 矩阵一致；写=manualConfirm 4 个（remove_book/suggest_source_fix/set_source_enabled/set_setting）；前置守卫 ctx.allowConfirm=false 时直接拒 | ToolRegistryFactory.kt:12-39 / ToolExecutor.kt:53-58 |
| 预设 | 13 家（deepseek/tongyi/zhipu/moonshot/siliconflow/minimax/doubao/openai/openrouter/groq/xai/ollama/lmstudio；ollama/lmstudio needsKey=false） | AiProviderPresets.kt:18-120 |
| 配置键 | ai_* 实为 14（PreferKey.kt:66-85；文档称 15）；ModelManager.getConfig 读 7 键+AiKeyStore 取 key，pref*Safe 类型异常删键自愈 | ModelManager.kt:29-43,63-76 |
| 后台任务 | AgentTaskCenter：进程级 SupervisorJob（无 Service 组件）、单任务槽、快照、>3s 发系统通知；开始/结束各落库一条文本（唯一持久化点） | AgentTaskCenter.kt:55-178 |
| 会话 | ConversationService：window 默认 50；loadChat=windowLatest(DESC)+reversed（P0-3 已修）；seq=maxSeq+1；>12000 字符从头裁剪；实体 AiSession/AiMessage（kind 含 confirm 两类预留） | ConversationService.kt:15-82 |

### 3.2 业务核心/数据层地图（fork 侵入面）

- 规则引擎 analyzeRule：AnalyzeRule(646 行)/AnalyzeUrl(455 行) + 4 后端（JSoup/XPath/Regex/JSONPath）+ Debug；model/webBook、localBook、rss 独立目录。
- 阅读引擎：ui/book/read/ReadBookActivity(866 行) + page/(ContentTextView 617 行等) + ReadMenu；ReadBook(409 行) 在 service/help 侧。
- 服务面：Service×6（AudioPlay/BaseReadAloud/CheckSource/Download/HttpReadAloud/TTSReadAloud）+ WebService（默认端口 1122，WS 1123，NanoHTTPD 9 条路由）+ ContentProvider ReaderProvider（uriMatcher 9 路径，exported）。
- **fork 对上游基座的侵入（决定性实测，git diff --name-only 401581f61..HEAD）**：业务核心 Java 文件（model/analyzeRule·webBook·localBook·rss、help/、service/、web/、api/、base/、lib/）**零改动**；非 ai/ 的全部 19 个 Java 改动 = 数据新增（AppDatabase/AiSession/AiMessage/DAO×2/Migration19To20/PreferKey）+ UI 入口（MainActivity/MyFragment/Config*/AiConfigFragment/SearchActivity/BookSource*/ReadMenu/TitleBar/ContextExtensions）+ App.kt。资源：layout 20、drawable(-night) 31、values(-night/zh) 若干、menu 2、xml 2（pref_config_ai/pref_main 新增）、assets/readConfig.json、src/ai strings。⇒ **rebase 上游的代码冲突面远小于预期**（核心层零侵入），主要冲突在 AI 功能与上游 UI 结构差异；但仍不推荐自动 merge（上游五年演进语义差异巨大）。
- 数据层：Room v20；17 DAO；@Database 实体 18（AppDatabase.kt:20-27）；entities/ 目录 22 文件（含非 @Entity 与规则子目录条目——文档 22 口径即由此误读）；迁移链 10→20 内联+独立文件 Migration19To20.kt；⚠️ fallbackToDestructiveMigration（迁移失败清库）与 allowMainThreadQueries（主线程查询许可，ReadRecordActivity 等小页同步读删）。
- 存储/缓存实况（核实）：无上游后期的 CacheManager/Directory 分层存储；help/storage/ = Backup/Restore/Preferences/WebDavHelp/ImportOldData(+Old*)（备份/迁移/同步）；缓存仅 utils/ACache.kt 底层文件缓存；图片经 help/ImageLoader；帮助面 help/ 其余为 AppConfig/CrashHandler/BookHelp/SourceHelp/ReadBookConfig/ReadTipConfig/coroutine/http/permission 子目录。JsExtensions（书源 JS 扩展，Rhino 系）在 help/ 供规则引擎使用（具体接线以 08-24 reports 为准）。

### 3.3 UI/主题层（关键事实）

- UI 包：ui/ 顶层 15 子包、235 个 kt（book 下 explore/search/info/read/source 等深链；widget 自定义 View 约 16 个 + 阅读 page 引擎 + AI 三件）。
- 设计令牌：颜色**半 token 化**——全 res xml 811 处 hex：values 426（调色板定义，合理）+ drawable(-night) 374（形状内硬编码）+ layout 11 + kt 10；`@color/` 219 次仅 26 个不同名。**尺寸无 token**：`@dimen/` 全布局仅 9 次 vs 内联 dp 1136 次（layout-land 66、kt 36）；dimens.xml 约 40 条多为 fastscroll 专用 → 字号/间距/圆角体系散落硬编码。
- 色板文件：values(-night)/colors.xml（71 hex，「现代暖纸质感(参考 olib-mobile)」，primary #2D6A4F）；colors_material_design.xml（268 hex，MD 经典色卡）；AI 专色 colors_ai.xml + values-night（各 29 令牌，注释自述映射「Book's Story BlueTheme」→ M3 令牌体系）。
- 阅读配色：assets/readConfig.json = 5 槽 × {bgStr/bgStrNight/textColor/textColorNight/bgType/bgTypeNight/darkStatusIcon/darkStatusIconNight}（Flexoki 等色值逐一吻合）；机制 ReadBookConfig.kt:21-67（filesDir 用户副本优先→已有副本的老用户不自动获得新种子，待确认），styleSelect 键 readStyleSelect（:116-122）。主题名（Flexoki/Everforest/Rosé Pine/Nord/Catppuccin）不出现在代码/资源中，仅 README/提交信息。
- 日/夜：AppConfig.isNightTheme → App.applyDayNight(kt:139-160: ReadBookConfig.upBg+applyTheme+setDefaultNightMode) + RECREATE 事件；ATH/ThemeStore（lib/theme，Phonograph 系）在 BaseActivity:102-117 按 Theme 枚举应用。
- 近期提交：eec4139cd 毛玻璃 = GlassEffect 纯软件盒式模糊（降采样最长边 128 → 3 趟滑窗 radius6 O(n)，因 compileSdk29 无 RenderEffect/弃 RenderScript 的全 API≥21 方案；onResume 一次性+lastBgPath 缓存+decodeSampled 防 OOM）+ layer-list 渐变浮卡（顶栏 22dp 圆角/输入卡 28dp 胶囊/1dp 高光描边，日夜 drawable 双轨）+ ReadMenu 运行时 GradientDrawable（alpha .88→.72、22dp、0x33FFFFFF 描边）。风险：主线程一次性模糊瞬态内存尖峰；AuroraBackgroundView 每帧重绘（仅 Hub 且默认关）。
- 0eb8eea85 五套配色：只改 assets/readConfig.json + README（零代码改动）。「一键切换」：全局日夜钮实存（ReadMenu.kt:209-213、ThemeConfigFragment），但「五套配色数值→界面主色」无映射代码（见 D17）。
- 空/载/错态：无统一组件（各页独立 TextView/字符串）；AI Hub 反向最结构化（6 行型 ai_item_{user,ai,tool,error,confirm,process} + 思考动画 + ProgressBar）。Markdown 渲染为 spec 待办（ai_item_msg_ai 为 textIsSelectable TextView）。
- WebView 5 处：help/http/AjaxWebView、HttpHelper、analyzeRule/AnalyzeUrl、ui/login/SourceLogin、ui/rss/read/*；mixedContentMode=ALWAYS_ALLOW ×2（AjaxWebView.kt:60、ReadRssActivity.kt:125）。
## 4. 数据流与调用图

### 4.1 App 启动链（App.kt 实测）
```
App.onCreate → INSTANCE/androidId → CrashHandler.init → LanguageUtils.setConfigurationOld
→ db = AppDatabase.createDatabase(v20, 迁移10→20, destructive fallback, allowMainThreadQueries)
→ versionCode/Name → 通知渠道(≥O) → applyDayNight → LiveEventBus.config(supportBroadcast)
→ registerActivityLifecycleCallbacks(ActivityHelp) → AiLog.attach → AiPlatform.init(5桥+22工具+syncConfig)
```

### 4.2 AI 调用图
```
AgentHub UI ──send──▶ ConversationService(会话) / AgentTaskCenter(后台槽) ──▶ AgentRuntime
    ▲ 90/150ms 渲染轮询·三级差分 · VM 80ms 工具事件+120ms 确认轮询 (StateFlow.value)
    └────────── onPartialText(SSE打字机)/ToolEvent/ConfirmRequest/AgentResult
AgentRuntime 多轮: repeat(maxRounds=5){ stopRequested?; completeOnce(SSE或非流式);
  无原生calls且非native→TextToolCallParser; 追加assistant; billed+=usage|256+len/3;
  超16k→BUDGET_EXCEEDED; 无工具→DONE }   HISTORY_CAP=24
工具流水线: resolve整批→并行invoke(单源8s/15s预算,写不重试)→按序回填tool
写工具: PENDING_CONFIRM→ConfirmRequest(call.id)→ApprovalBus.await(token,≥300s,stop)→
  同意→def.onApproved 真写库 / 拒|超时|stop→NO_PERMISSION 回填
文本协议: SystemPromptBuilder 注入XML协议+工具清单 → 模型XML输出 → parser→ToolCallData
桥接边界: 工具→AiBridge接口(注入) → Default*(BookFetcher/ChapterReader/SourceAnalyzer/
  AppController/SourceRuleWriter) → App.db / WebBook（桥层直接触达, AI不直连仅在tool层成立）
```

### 4.3 阅读/书源主链路（上游基座概貌）
- 书源流：BookSourceDao → 规则链 AnalyzeUrl/AnalyzeRule(+4后端) → WebBook 流水线（搜索/目录/正文）→ Book/BookChapter 落库 → 阅读引擎 ReadBook(service/help) → 翻页 ContentTextView/PageView。
- AI 只经桥访问：search 随机抽 6 源、单源 8s 超时；章节读 15s；书源测试搜「我的」15s；写操作白名单字段合并（TOP_FIELDS/RULE_GROUPS）。
- 核心链路结构以 reports/2026-08-24 两份精读为准（主控已复核其 LOC 命中：AnalyzeRule 646/AnalyzeUrl 455/ReadBook 409/ReadBookActivity 866/ChapterProvider 462/ContentTextView 617/ReaderProvider 271/EventBus 19 tags/base 1187/receiver 200/lib 2909/web 436/service 含 help 2540/model 4168/help 4901/UI 33685）；WebBook 内部步骤与 ReadBook 状态机逐文件细节见该两份报告，未在本次逐行复核处标「待确认」。

## 5. 安全边界图与风险面

```
┌─ 进程内 ───────────────────────────────────────────────────┐
│ AI工具→桥接口→领域: 读自由/写需确认(manualConfirm×4+守卫)      │
│ API Key: AndroidKeyStore AES/GCM(降级: SDK<23/异常→明文+告警) │
│ AiLog: 无完整请求体·Key全mask·baseUrl未脱敏                    │
│ 模型上下文可能含完整书源JSON(含header Cookie字段)→隐私面        │
├─ App边界 ──────────────────────────────────────────────────┤
│ ReaderProvider: exported=true 无任何权限保护（api.md 声称的    │
│   io.legado.READ_WRITE 权限不存在）                           │
│ Web服务(NanoHTTPD:1122): 无token·CORS反射任意Origin·书源增删改 │
│ WebView: mixedContent ALWAYS_ALLOW ×2（Ajax/RSS 所需）        │
│ 书源=网络内容驱动规则/脚本 → 固有 RCE 面（产品定位）             │
│ 权限13项(上游遗留 READ_PHONE_STATE/GET_ACCOUNTS)               │
├─ 供应链 ────────────────────────────────────────────────────┤
│ legado.jks 入库+口令明文(gedoor_legado) → 可伪造同签名包 ⚠     │
│ legado.yml: watch:started(任意star触发) + 每日cron            │
│ 密钥扫描: 全树+全历史仅 google-services.json Firebase key(信息级)│
└──────────────────────────────────────────────────────────────┘
```

### 5.1 风险表（S1-S10：主控实测 + sec/ui/ai 深读路实证）
| # | 风险 | 级别 | 位置/证据 | 缓解建议 |
|----|------|------|-----------|----------|
| S1 | 签名 keystore 入库+口令明文，零 Secrets | 高(供应链信任) | .github/workflows/legado.jks(2232B, 551410a30) + ai-build.yml:43-44 / legado.yml:32-36 | 换独立 keystore 走 GitHub Secrets（版本化迁移防断升级链）；至少把口令改密 |
| S2 | ReaderProvider exported=true 无鉴权 | 高 | Manifest:369-375；api.md:86 声称的 READ_WRITE 权限不存在 | 若无需跨应用则 exported=false；否则加 signature 权限 |
| S3 | Web 服务无 token+CORS 反射+可写书源 | 中高 | WebService.kt:99(1122)/HttpServer；controller/SourceController | 可选 token+Origin 白名单；保持默认关并提示局域网风险 |
| S4 | AI Key 明文降级路径 | 中 | AiKeyStore.kt:72,82(SDK<23 恒明文),37-39,62-66 | 现状可接受（降级面小且有日志）；后续可考虑 API21/22 禁用 AI Key 保存 |
| S5 | WebView mixedContent 全放行 | 中 | AjaxWebView.kt:60 / ReadRssActivity.kt:125 | 书源 http 场景所需；文档化威胁模型 |
| S6 | 书源 JSON（含 Cookie header 字段）进模型上下文 | 中(隐私) | bridge 实现 | 诊断类工具脱敏 header 再送出（后续） |
| S7 | 上游遗留权限/star 触发 CI/公开签名资产 | 低-中 | Manifest 全量 / legado.yml:7-15 | 清理无用权限；移除 watch 触发；cron 保留评估 |
| S8 | AiLog baseUrl 未脱敏；会话导出含全部对话 | 低 | AiLog.kt（Key 已 mask） | baseUrl 属半敏感，可视需要 mask query 部分 |
| S9 | 数据安全：destructive migration + 主线程查询 | 中(数据/性能) | AppDatabase.kt createDatabase | 移除 allowMainThreadQueries 评估；迁移失败 UI 警示 |
| S10 | 会话删除不停运行中任务 → 孤儿行（无 FK） | 低 | AgentHubViewModel.kt:221-235 / DAO | deleteSession 前 ensureIdle（对齐 spec） |

## 6. 文档-代码一致性（漂移与缺口）

### 6.0 结论先行
1. **无断链**：README/深度理解引用的全部 md（含 08-23 rikkahub/operit/ambient specs）均在库且被 git 跟踪；旁支（release/ai-agent-v1、trae/* 等 8 个）均无 docs/。此前一次文件系统枚举异常造成的「断链」误判已撤销。
2. **漂移集中在统计口径与机制行文**（22 项 D1-D22），其中机制级/功能级 4 项：确认窗口 120s→≥300s（D4）、Default 桥 4→5（D3）、一键切换语义（D17）；高严重度口径 3 项：fork 增量（D1）、实体 22→18（D6）、api.md READ_WRITE 权限不存在（D18）。
3. **README 超前声明 1 项**：五套主题名与「阅读→界面一键切换」的完整语义无代码支撑（配色数值→界面主色无映射）。

### 6.1 漂移表（D1-D22）
| # | 文档位置 | 声称 | 实际（HEAD=eec4139cd） | 严重度 | 证据 |
|----|----------|------|--------------------------|--------|------|
| D1 | 深度理解§1 | fork 79 提交/124 文件/+6792/-80/41ai | 86/163/+11212/-128/44；79/124/41 在撰写点 7bdfef75f 附近亦不可精确复现（41 在 4333845eb/7bdfef75f 成立） | 高 | git log/diff |
| D2 | 深度理解§1/2 | ai ≈4700 行/38 文件 | 44 文件/5021 行；「38 文件」任何提交都不成立（7bdfef75f=41/4694）且与同文档 §1 矛盾 | 中 | git ls-tree+wc |
| D3 | 深度理解§3.1 | 4 个 Default* bridge | 5 个（P0 增 DefaultSourceRuleWriter；§7 P0-2 自述但正文未同步） | 中 | AiPlatform.kt:49-55 |
| D4 | 深度理解§3.2 | 确认超时=timeoutMillis 默认 120s | 确认窗口独立：默认 300s，装配 maxOf(timeout,300s)（120s 系 OpenAIClient 请求超时，机制混写；§7 P1-5 自述已修但正文未同步） | 高(机制) | AgentRuntime.kt:42,303-305/AiPlatform.kt:88/OpenAIClient.kt:28 |
| D5 | 深度理解§3.1 | 16k @AiPlatform.kt:83 | 装配传 16_000 属实，行号 :86-87；runtime 默认参数 32k 被覆盖 | 低 | AiPlatform.kt:86-87 |
| D6 | 深度理解§2 | 22 实体 | @Database=18 @Entity；22=entities/ 目录条目数误读（含非实体/子目录） | 高(口径) | AppDatabase.kt:20-27 + ls |
| D7 | 深度理解/README | ai_* 键 15 | 14（PreferKey.kt:66-85） | 低 | grep |
| D8 | 深度理解§1 | versionCode 5757+ | 机制对；HEAD=5771；APK 5757 实构建于父提交(rev-list 5758) | 低 | git rev-list |
| D9 | README/指南 | 仓内 APK 最新 | 落后 HEAD 14 提交 | 中 | commit 对比 |
| D10 | README | 22 工具/13 预设/包名/SDK/双轨等 | 全部成立（逐项核对） | 无 | 多文件 |
| D11 | 深度理解 | 启动链描述 | 成立（行号 46-70 → 现 45-73 区间） | 无 | App.kt |
| D12 | （前期误判） | README 两 spec 断链 | 在库且跟踪；疑点前提不成立 | 无 | git ls-files |
| D13 | specs | ambient spec 未收录于 README | 在库（规划态无标识） | 低 | 目录 |
| D14 | CHANGELOG | 1.0.0(2020-02-09) 模板 | fork 后从未维护 | 低 | 文件 |
| D15 | api.md | Web 端口 1234 | 默认 1122（可配置 webPort） | 中 | WebService.kt:99-101 |
| D16 | 深度理解§7 | P0/P1 修复自述 | 代码吻合（windowLatest/ApprovalBus/DefaultSourceRuleWriter 均在） | 无 | 多文件 |
| D17 | README L36 | 「阅读→界面 一键切换」 | 部分成立：全局日夜钮实存（ReadMenu.kt:209-213/ThemeConfigFragment）；但五套配色数值→界面主色无映射；主题名仅 README/commit | 中(口径) | ui 深读 + git show 0eb8eea85 |
| D18 | api.md L86 | 需声明 io.legado.READ_WRITE 权限 | 全仓不存在；provider 实际无保护 | 高 | grep 0 命中 + Manifest:369-375 |
| D19 | specs 08-20 | EncryptedSharedPreferences | 实现=自研 AiKeyStore（无 androidx.security 依赖）；偏离未标注 | 低 | build.gradle + AiKeyStore.kt |
| D20 | plans×2 | 实施计划 checkbox | 目标文件全建但 checkbox 全未回填；Task4 伪代码→13 预设表 | 低 | plans md |
| D21 | specs 08-20 消息持久化 | tool/confirm 消息落库 | 运行时只落 user/assistant（唯一写点 AgentTaskCenter.kt:102/118）；实体/DAO 已就绪未接线；archive() 零调用；技能开关/@ReadOnly 未落地 | 中(实现差距) | ConversationService/AiMessage 设计 vs AgentTaskCenter |
| D22 | README 工具矩阵 | explain_text 为真实能力 | **占位实现**（回显 text 前 500 字，无桥调用） | 中(功能声明) | BookReadingTool.kt:107 |

### 6.2 文档时效判定
| 文档 | 最后事实更新 | 判定 |
|------|--------------|------|
| README.md | 0eb8eea85 08-24 17:26 | 落后 1 提交（毛玻璃未覆盖）+ 超前声明（D17/D22） |
| 项目深度理解.md | ebbfaec6a 08-24 16:37 | 落后 2 提交；头部统计与 §3 机制行文过期（D1-D7） |
| 小白上手指南.md | 3f3541d3c 08-23 | 安装/配置向，仍有效；建议指向最新 Actions artifact |
| specs ×4 | 各自落地提交 | 设计文档：rikkahub/operit/ambient 已落地；08-20 部分设想未落地（D19/D21，已知差距） |
| plans ×2 | 08-20 | checkbox 未回填（D20） |
| reports ×2 | 4333845eb | 行数口径微漂（ui -265、ai +3 文件），其余精确 |
| api.md | 2020-08-10（fork 前） | 上游遗留：D15/D18 过时 |
## 7. 重构优先级图

```
P0（影响正确性/信任）                        P1（影响质量/效率）
 ├─ S1 签名迁移: Secrets/独立keystore(带版本迁移) ├─ 本文档与 D1-D22 修订入库(本次)
 ├─ S2 ReaderProvider exported 收紧               ├─ D17 语义落实或 README 收敛口径
 ├─ S3 Web 服务 token + Origin 白名单             ├─ D22 explain_text 落地或标注占位
 ├─ S9 主线程 DB 访问收敛评估                     ├─ UI 尺寸 token 化(内联 dp 1136 处)
 └─ R1 上游同步策略专项(合并 or 冻结)              └─ 发布流水: 版本化签名/自动说明/APK 对齐
P2（长期演进）
 ├─ 文档例行核对机制(§10 Checklist 脚本化)         ├─ spec D21: 消息持久化/技能开关/archive 接线
 ├─ synthetics(172)→ViewBinding 渐进               ├─ 技术栈升级专项(先可行性报告, 禁直接升)
 └─ 分层模块化(先文档化边界, 禁强拆)               └─ WebBook/analyzeRule 单测补齐
禁止: 大规模 rebase 上游 · 一次性推翻重写 · 为现代化拆模块 · 未跑迁移改 Room schema
```

## 8. Risks（风险清单）

| 风险 | 等级 | 说明/对策 |
|------|------|-----------|
| R1 上游迁移 | 高 | 基线 2020-08+深度改造；autoupdatefork 已停用定时（自述盲 merge 必大规模冲突）；对策：冻结+定向 cherry-pick，如迁移开专项分支评估（D1 的 86 提交含 10 个 2020 merge 说明历史上曾试合并上游） |
| R2 工具链老化 | 高 | JDK11 钉死/AGP 4.0.1/jcenter 停服（靠 aliyun/jitpack 镜像）；minify 全关；对策：容器化锁定+升级专项 |
| R3 coroutines 声明矛盾 | 中 | 声明 1.3.7 却用 MutableStateFlow（1.4+ API）；ApprovalBus 却因「1.3.x 无 SharedFlow」改轮询（ApprovalBus.kt:9-11 注释自陈）；真实解析版本待确认（禁构建）→ 需 gradle dependencyInsight（证据：ApprovalBus.kt + app/build.gradle:165-167） |
| R4 数据丢失 | 中 | destructive migration fallback；AI 确认文案防误导（README 已警示） |
| R5 文档过期 | 中 | 本文档=DDD 起点：README/深度理解/报告需按 §10 例行核对 |
| R6 发布滞后 | 中 | 仓内 APK 落后 14 提交；以 Actions artifact 为准 |
| R7 单任务槽/孤儿写 | 低-中 | AgentTaskCenter 单槽+deleteSession 不停任务（D21/S10） |
| R8 AI 能力扩展失控 | 中 | 新增工具必须过桥接口+manualConfirm 审计；工具注册集中在 ToolRegistryFactory 便于审查 |

## 9. 阶段 3 判断（最该做/最不该做/延后）

1. **最该做**：本次交付（报告入库+README 链接+api.md 注释）→ 随后 S1/S2/S3 专项设计文档（安全面）→ 文档例行核对机制。
2. **最不该做**：动上游基座核心（除安全修复）；rebasing gedoor/legado；更改 AiBridge 契约/pending_confirm 状态机/Room schema（必须走 Migration）；改三协议兼容格式（Operit 生态兼容是卖点）；未评估先升技术栈。
3. **最该先补文档**：AI 写操作面与桥契约（工具=能力的边界清单）；UI 令牌边界（colors_ai/colors_material_design/colors.xml 用途与迁移）；发布/签名/升级链说明；书源威胁模型。
4. **可以延后**：模块化拆分、Compose、UI 全量重绘、markdown 渲染、技能开关等 spec 设想。
5. **不能贸然改动**（红线）：AiBridge 接口形状、ApprovalBus token 语义、Room schema（改实体必须 Migration+版本号）、签名配置（破坏升级链）、AI Key 存储链路（P0 修复成果）。

## 10. Checklist（下次调研/刷新核对表）

- [ ] HEAD 是否变化（git log -1）；versionCode=git rev-list --count HEAD
- [ ] 文档链接批量校验（md 相对链接全检）；本文档漂移表 D1-D22 复跑
- [ ] 统计脚本：ai/ 文件数与行数、工具 register 数、AiProviderPresets 条数、PreferKey ai_* 数、@Database 实体数
- [ ] Room version 与 migration 链；新增表必须带 Migration 检查
- [ ] manualConfirm 工具清单与 README 🔐 一致性；新工具是否走桥+确认
- [ ] 仓内 APK versionCode vs HEAD 差距；Actions artifact 状态
- [ ] 密钥例行扫描（sk-/ghp_/github_pat_/口令模式）；origin/credential 卫生
- [ ] coroutines 解析版本确认（R3）；explain_text 占位状态；archive/技能开关接线状态

## 附录 A：证据索引（代表性 文件:行）

- 启动/装配：App.kt:45-73,139-160 · AiPlatform.kt:47-58,86-88 · AgentHubViewModel.kt:95-103,320-340
- AI 运行时：AgentRuntime.kt:39-42,56,94-97,105-108,126-146,193-230,256-288,303-305 · OpenAIClient.kt:28,34-38,76-171,221-264 · ApprovalBus.kt:9-43 · ToolExecutor.kt:53-67 · AgentTaskCenter.kt:55-178 · ConversationService.kt:15-82
- AI 存储/日志：AiKeyStore.kt:27-82 · AiLog.kt:30-102 · ModelManager.kt:29-43,63-86
- AI 工具/桥：ToolRegistryFactory.kt:12-39 · ToolRegistry.kt:30-33 · TextToolCallParser.kt · AiBridgeImpl.kt:23-247 · DefaultAppController.kt · DefaultSourceRuleWriter.kt:20-71 · BookReadingTool.kt:107（explain_text 占位）· BookSourceTool.kt · AppControlTool.kt · AiProviderPresets.kt:18-120
- 数据：AppDatabase.kt:20-27,36-55 · PreferKey.kt:66-85 · data/migrate/Migration19To20.kt · AiMessageDao.kt:12-25
- UI/主题：ui/ai 6 文件 · GlassEffect.kt:6-10,13-77 · AgentHubActivity.kt:119-148,253-299,582-601 · ReadMenu.kt:194-213 · ReadBookConfig.kt:21-122 · colors_ai.xml · colors_material_design.xml · readConfig.json（5 槽）· activity_book_read.xml:41（悬浮球挂载）
- 构建/CI/安全：app/build.gradle:11-24,34-39,77-124,157-167 · AndroidManifest.xml:147-151,369-375 · WebService.kt:99-103 · AjaxWebView.kt:60 · ReadRssActivity.kt:125 · .github/workflows/{ai-build.yml:43-44, android.yml, legado.yml:32-36, autoupdatefork.yml:1-12}
- 关键提交：401581f61(fork 点 2020-08-31) · 3f3541d3c(5757 包) · 7bdfef75f(深度理解) · 26e64265e(P0) · ed991c457(P1) · ebbfaec6a(单测+scope) · 3e2364e62(coroutines 兼容) · 0eb8eea85(五套配色) · eec4139cd(毛玻璃 HEAD)

## 附录 B：待确认清单（禁编造项，下轮核销）

1. coroutines 实际解析版本（R3）——需 gradle dependencyInsight（禁构建约束下未验证）。
2. 五套配色 MIT 许可归属与主题名映射——需外网核对（色值已逐一吻合）。
3. WCAG AA：正文/背景 10 对已实测达标（5.18-13.39:1）；组件级/次级文字未逐色验证。
4. assembleAi*/testAiDebugUnitTest 任务名由 AGP 规则推断+CI 佐证，本地未实跑。
5. 老用户 readConfig 用户副本是否普遍存在（决定 0eb8eea85 是否惠及老用户）。
6. UI 尺寸断言（气泡 20dp/胶囊 28dp 等）未逐 drawable 比对。
7. （已自行核销）fork 非 ai 改动精确清单——见 §3.2（19 个 Java 文件名单已列）。WebBook/ReadBook 内部细读仍以 08-24 两份 reports 为准。

---

*本报告由主控代理基于五路深读子代理笔记（.survey-legado-2026-09/{docs-audit,ai-layer,build-ci-security}.md 与 ui-layer 内联笔记）与主控独立复核汇编；所有量化声明可回溯到附录 A 证据或 git 命令。*
