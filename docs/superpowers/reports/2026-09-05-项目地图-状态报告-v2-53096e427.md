# legado（Arisemoss/legado）· 项目地图与状态报告 v2（新基线）

> **调研基线**：`53096e427`（master，2026-09-05 20:28，versionCode = git rev-list --count HEAD = 5795）
> **报告日期**：2026-09-05　**性质**：总控流程 阶段1~2 调研产物 v2——针对基线迁移后的刷新
> **缘起**：v1（基线 `eec4139cd`）调研期间远端 master 被同栈重放+增量演进（31 文件/+3006/-128），v1 已存档于分支 `docs/map-report-2026-09-baseline-eec4139cd`（commit f926ffed8）。本文档以**新基线真值**重写，v1 中未变的结论直接沿用并标注出处。
> **证据规则**：结论附 文件:行 / commit / git 命令；无法核实标「待确认」，禁止编造。

---

## 0. TL;DR（一页结论）

| 维度 | 结论 |
|------|------|
| 基线 | 53096e427 = 同栈（Kotlin 1.4/AGP 4.0.1/SDK29/Room 2.2.5/单模块）重放的 AI fork + 08-31~09-05 增量：**31 文件 / +3006/-128**；ai/ 层与 eec4139cd 逐字节一致（44 文件/5021 行，diff=0） |
| 代码健康度 | 增量以安全加固与性能修复为主：签名 Secrets 化+jks 出库、ReaderProvider signature 权限、Web 服务默认 127.0.0.1+局域网开关、release R8 混淆+AI keep、HttpHelper TLS 默认系统 CA+LRU Retrofit 缓存、章节预取可取消 |
| 安全态势 | v1 高危 S1/S2 代码面已修复、S3 部分修复（绑定收敛）；遗留：签名密钥仍可从 git 历史检出（需轮换/清史）、S4/S5/S6/S9/S10 未动、release R8 属新增回归面 |
| 文档 | 新增 6+1 份 Draft（docs/{ai,architecture(×3),ui,performance} + project-improvement-plan spec）；**项目深度理解 §7 已随代码更新，但头部统计行（38/4700、22 实体、15 键、120s）仍陈旧**；api.md 未跟进新权限模型 |
| 最优先下一步 | 1) 本文档入库 + README 链接（本次）；2) 深度理解头部统计/机制行文同步（D 项复跑）；3) 签名 keystore 轮换+历史清理评估；4) S9 数据面与 R3 coroutines 专项 |

## 1. Background（谱系与两次基线）

- fork 谱系：gedoor/legado（source）→ Carl-Wen/legado（parent）→ Arisemoss/legado。上游快照基线 2020-08-31（fork 点 401581f61）。
- **基线事件**：调研期间远端 master 从 eec4139cd 被同栈重放（全部 AI fork 提交换哈希重放）+ 追加增量提交（08-31 起），HEAD 现为 53096e427。旧 eec4139cd 内容在本仓已无分支指向（v1 报告已存 `docs/map-report-2026-09-baseline-eec4139cd` 分支）。
- 增量提交时间线（git log 实查）：
  - 08-31 09:30 `364e5cc65` security：P0 安全项批量修复 + release R8（详见 §5）。
  - 08-31 `217bb355b` fix：章节预取可取消专用 scope（ReadBook/ReadBookActivity/OtherConfigFragment）；`29d0fd799`/`c49091a2d` fix(ci)（readBodyToLh/保留字 _）。
  - 08-31 `eb038b0d5` perf：HttpHelper Retrofit 按 (baseUrl, encode, proxy, 转换器) 有界 LRU 缓存，refreshClient 重建即失效。
  - 09-01 `c4b8b0333`~`af3d151d3` chore：web 前端资源打包产物入库（bookshelf/index HTML zip）；`22d0c4633`/`2b09e81ec` chore: pre-termination backup。
  - 09-05 20:11-20:28：`b51e9db82` Add test-chatgpt-write.txt → `7d0f64343` Delete 之（试写）；随后 6 个 docs 提交：project improvement/UI plan（e3ca24285）、architecture design blueprint（9f83156cc）、research-backed roadmap（97e38552e）、module boundary（a9ca3d72a）、AI platform architecture（1dde1ff72）、UI design system（b61f1b616）、performance optimization guide（53096e427=HEAD）。
- 增量文件构成（eec4139cd..53096e427，31 文件）：Java 9（PreferKey/AppConfig/HttpHelper/WebService/ReadBook/ReadBookActivity/OtherConfigFragment/HttpServer/WebSocketServer）；资源 5（build.gradle/Manifest/res×3）；workflows 4 变更（ai-build/android/autoupdatefork/jks 删除+legado.yml 删除）；docs 7 新增/更新；strings 2。

## 2. Current State（仓库现状 v2）

### 2.1 版本与构建身份（实测不变项 + 变更项）
- 包名/版本公式/SDK 与 v1 一致：flavor app(io.legado.app)/google(io.legado.play)/ai(io.legado.ai)+buildType 后缀；versionName=`3.yy.MMddHH(GMT+8)-ai-v2`；versionCode=`git rev-list --count HEAD`（现 5795）。
- **变更**：release `minifyEnabled=true`（R8，app/build.gradle:59），proguard-rules.pro +4（含 `-keep io.legado.app.ai.**` 防反射被混淆）；debug 仍 false。
- **变更**：签名不再由仓库内 jks 提供——`.github/workflows/legado.jks` 与 `legado.yml` 已删除（364e5cc65）；CI 由 Secrets（LEGADO_RELEASE_KEYSTORE_BASE64/KEY_ALIAS/STORE_PASSWORD/KEY_PASSWORD）注入，未配置则构建 unsigned 并告警（ai-build.yml:44-59）。⚠️ 历史仍含旧 jks/口令对象，见 §5 S1。
- workflows 现 3 条：ai-build / android（fetch-depth:0 修复 versionCode 失真）/ autoupdatefork（仅手动）。
- **变更**：PreferKey 新增 `allowUnsafeCertificate`、`webLanAccess`（非 ai_*，ai_* 仍 14 键）。

### 2.2 文档清单（master，docs/ 18 文件）
```
docs/项目深度理解.md（§7 已随 364e5cc65/217bb355b 更新；头部统计行仍陈旧→D 表）
docs/小白上手指南.md · api.md（根目录，未跟进新权限模型→D 表）
docs/ai/AI_PLATFORM.md          Draft 1（461 行）
docs/architecture/ARCHITECTURE.md Draft 1（444 行）
docs/architecture/MODULES.md    Draft 1（544 行）
docs/architecture/ROADMAP.md    Draft 2（367 行，Compose-first 目标）
docs/ui/DESIGN_SYSTEM.md        Draft 1（355 行）
docs/performance/PERFORMANCE.md Draft 1（346 行）
docs/superpowers/specs/2026-08-20-ai-agent-platform-design.md
docs/superpowers/specs/2026-08-23-{ambient-ai-reader,operit-toolcall-compat,rikkahub-ui-migration}.md（在库，README 链接有效）
docs/superpowers/specs/2026-09-05-project-improvement-plan.md（235 行）
docs/superpowers/plans/2026-08-20-{ai-agent-platform,app-action-bus}.md
docs/superpowers/reports/2026-08-24-{model-help,ui-service-web}-深度精读.md
（本 v2 报告入库后 +1）
```
## 3. Architecture（分层架构总览 v2）

### 3.0 不变项（v1 §3 全量有效，ai/ 与核心层 diff=0）
- AI 平台层 io.legado.app.ai：44 文件/5021 行，结构与 v1 逐字节一致（工具 22/写 4、预设 13、ai_* 键 14、AndroidKeyStore AES-GCM、ApprovalBus ≥300s 确认窗、三协议、AiLog 800+512KB、AgentTaskCenter 单槽等，证据同 v1 附录 A）。
- 业务核心层（model/analyzeRule·webBook·help·service·web·api·base·lib）零侵入结论不变；Room v20/17 DAO/18 @Entity/entities 22 文件口径不变；fallbackToDestructiveMigration + allowMainThreadQueries 现状不变（S9）。
- 阅读配色/毛玻璃/主题体系（readConfig 5 槽、colors_ai、M3 令牌、GlassEffect）与 v1 §3.3 一致。

### 3.1 增量代码面（31 文件中的 9 个 Java + 2 配置）
| 文件 | 变更要点 | 提交 |
|------|----------|------|
| help/http/HttpHelper.kt | TLS 默认系统 CA；`allowUnsafeCertificate` 开关；OkHttpClient/Retrofit 按 (baseUrl,encode,proxy,转换器) 有界 LRU 缓存；refreshClient() 重建 | 364e5cc65 + eb038b0d5 |
| service/WebService.kt + web/{HttpServer,WebSocketServer}.kt | 默认仅绑定 127.0.0.1；`webLanAccess` 显式开局域网 | 364e5cc65 |
| AndroidManifest.xml | 新增 signature 级 `io.legado.app.permission.READER_DATA`；readerProvider 挂 read/writePermission（去掉 tools:ignore） | 364e5cc65 |
| service/help/ReadBook.kt + ui/.../ReadBookActivity.kt | callBack onDestroy 置空（泄漏）；±10 章预取改可取消专用 scope，换书/关闭取消 | 364e5cc65 + 217bb355b |
| constant/PreferKey.kt | +allowUnsafeCertificate/+webLanAccess | 364e5cc65 |
| help/AppConfig.kt | 开关接线（allowUnsafeCertificate.setter→refreshClient） | 364e5cc65 |
| ui/config/OtherConfigFragment.kt | 预取取消入口相关 | 217bb355b |

## 4. 数据流与调用图

v1 §4 全部适用（App 启动链、AI 调用图、确认流、阅读/书源主链路），唯一变化：HttpHelper client 变为**可重建**（开关生效），网络路径多一层 LRU 缓存；无架构形状变化。指向 v1 §4（存档分支文件可查）。

## 5. 安全边界图与风险面（v2 更新）

### 5.1 风险状态表（相对 v1 的增量判定）
| # | 风险（v1 表述） | v2 状态 | 证据/说明 |
|----|----------------|---------|-----------|
| S1 | 签名 jks+口令明文入库 | **入口已修复，历史残留** | legado.jks/legado.yml 已删，CI 走 Secrets 且无明文（ai-build.yml:44-59）；但 git 历史对象仍含旧 jks+口令 → 需 keystore 轮换或历史清理（filter-repo） |
| S2 | ReaderProvider exported 无鉴权 | **已修复** | signature 级 READER_DATA 读写权限（Manifest:22-28,378-383） |
| S3 | Web 服务无鉴权+CORS 反射 | **部分修复** | 默认绑定 127.0.0.1（webLanAccess 开关）；仍无 token、CORS 反射任意 Origin 未收 |
| S4 | AI Key 明文降级路径 | 未变（开放） | AiKeyStore 降级分支仍存在（SDK<23/异常回退明文+告警） |
| S5 | WebView mixedContent 全放行 | 未变 | AjaxWebView.kt:60 / ReadRssActivity（2 处 ALWAYS_ALLOW） |
| S6 | 书源 JSON（含 Cookie 字段）入模型上下文 | 未变 | 诊断工具上下文脱敏未做 |
| S7 | 上游遗留权限/star 触发 CI | 部分修复 | legado.yml（watch:cron）已删；READ_PHONE_STATE/GET_ACCOUNTS 等权限未审 |
| S8 | AiLog baseUrl 未脱敏 | 未变（低） | — |
| S9 | destructive migration + 主线程查询 | 未变 | AppDatabase.kt（AppDatabase 不在增量内） |
| S10 | 删除会话不停任务→孤儿行 | 未变 | AgentHubViewModel deleteSession 未 ensureIdle |
| S11 | **新增**：release R8 混淆回归面 | 新增风险 | AI 反射/XML 工具路径依赖 -keep io.legado.app.ai.**（proguard-rules +4）→ release 包需回归（工具调用/确认流/流式） |
| S12 | **新增**：TLS 收紧的兼容面 | 新增风险 | 默认系统 CA 会拒绝自签名/旧 http 书源；已提供 allowUnsafeCertificate 开关，需引导老用户 |

## 6. 文档-代码一致性（v2 漂移表，聚焦新基线与新文档）

### 6.1 直接沿用 v1 判定（D10-D14、D16-D17、D19-D22 在新基线同样成立）
- D10 22 工具/13 预设/包名/SDK 等仍全成立（重测：register 22、ai_* 14、@Entity 18、Room v20）。
- D17 一键切换口径、D21 spec 消息持久化未落地、D22 explain_text 占位——代码未变，仍成立。

### 6.2 新基线漂移/缺口（新增与已消项）
| # | 位置 | 声称 | 实际 | 判定 |
|----|------|------|------|------|
| N1 | 项目深度理解 §2/§3 头部 | 38 文件/4700 行、22 实体、15 键、120s、Key 加密链路死 | ai 44/5021、18 @Entity、14 键、确认窗≥300s、加密链路已活（P0） | **仍陈旧**（364e5cc65 只更新 §7 与安全段，头部统计/§3 机制行文未同步） |
| N2 | 深度理解 §7（新旧基线差异） | v1 分支版含 P0/P1 ✅ 自述 | 新基线版把技术债 10-13 逐条 ✅/归档并补「归档/暂保留说明」（217bb355b/364e5cc65 后） | 新基线版**更接近现状**；建议 v1 结论以本版为准 |
| N3 | api.md | 端口 1234；需 io.legado.READ_WRITE 权限 | 默认 1122；权限不存在（现为 signature 级 READER_DATA） | **仍过时**（api.md 未在新基线改动） |
| N4 | 新 6+1 docs（Draft） | 面向未来架构/规范（Compose-first、模块化、AI 平台化、设计系统、性能规范、改进计划） | 属规划档：与当前代码（无 Compose、单模块）的差距即其目标本身；AI_PLATFORM/ARCHITECTURE 内具体实现断言需逐条对账（下轮专项） | 规划档（缺「现状对照/里程碑勾选」结构） |
| N5 | README 开发文档区 | 引用 08-23 三份 specs | **在库**（docs/superpowers/specs/ 5 文件），链接有效；v1 曾担忧断链不成立 | 无 |

## 7. 重构优先级（v2）

```
P0（本周级）                            P1（月度级）
 ├─ 文档同步: 深度理解头部统计/§3 行文  ├─ S4 AiKeyStore 降级收紧(可选)
 ├─ api.md 注记/更新                     ├─ S12 TLS 兼容引导与回归
 ├─ S1 后续: keystore 轮换+清史评估     ├─ N4 新 docs 现状对照章节
 ├─ S11: release(R8) 全量回归           └─ 发布流水: 版本化 release 说明
 └─ 本文档+README 链接入库(本次)
P2（长期）
 ├─ S9 主线程 DB/破坏性迁移 · S3 Web token/Origin 白名单 · S10 孤儿写
 ├─ R3 coroutines 声明矛盾解析 · explain_text 落地 · spec D21 接线
 ├─ ROADMAP 的 Compose-first/模块化/插件化分阶段（禁强拆）
 └─ 文档例行核对机制（§10 Checklist 脚本化）
禁止: rebase 上游盲合 · 推翻重写 · 未跑迁移改 Room schema · 动 AiBridge/确认机契约
```
## 8. Risks（v2）

| 风险 | 等级 | 说明/对策 |
|------|------|-----------|
| R1 双基线并存 | 高 | 旧基线（eec4139cd）已被本分支取代；历史仍可从对象库检出。对策：以 53096e427 为准，旧分支仅存档；必要时清史 |
| R2 签名历史残留 | 中高 | jks/口令对象仍在 git 历史（364e5cc65 删除的是 HEAD 指针）。对策：keystore 轮换为最小动作；完整清史需 filter-repo+全员强推 |
| R3 工具链老化 | 高 | 不变（JDK11/AGP4.0.1/jcenter 镜像依赖）；新增：release R8 后需回归 |
| R4 coroutines 声明矛盾 | 中 | 不变（1.3.7 vs StateFlow，禁构建未验证，需 dependencyInsight） |
| R5 文档滞后节奏 | 中 | 深度理解头部/§3 与 api.md 已滞后（N1/N3）；对策：本文档+例行核对 |
| R6 发布滞后 | 中 | ai-apk-download 仍是 5757 包，落后（5795-5757=38 提交当量）；以 Actions artifact 为准 |
| R7 新 docs 规划落差 | 低-中 | 6+1 Draft 的目标（Compose-first 等）与现状差距大；缺里程碑/现状对照 → 计划漂移风险 |

## 9. 阶段 3 判断（v2）

1. **最该做**：本 v2 入库 + README 链接；同步 深度理解头部统计与 api.md（N1/N3，本次一并做最小修正）。
2. **最不该做**：盲 rebase/merge 上游；重写历史（未评估前）；动 AiBridge/pending_confirm/Room schema；改三协议兼容格式；强拆模块。
3. **最该先补文档**：N4 新 docs 的「现状对照/里程碑」节；深度理解 §3 机制行文；发布/签名/升级链说明。
4. **可以延后**：Compose/模块化/插件化（ROADMAP 目标）、explain_text 落地、markdown 渲染。
5. **红线（不能贸然改）**：AiBridge 接口形状、ApprovalBus token 语义、Room schema（改实体必须 Migration）、签名配置（刚 Secrets 化，勿回退）、AI Key 存储链路。

## 10. Checklist（基线 53096e427 例行核对）

- [ ] HEAD 是否变化；versionCode=git rev-list --count HEAD
- [ ] ai/ 文件数与行数、工具 register 数、AiProviderPresets 条数、ai_* 键数、@Entity 数（脚本化）
- [ ] Room version/migration 链；新表必须 Migration
- [ ] manualConfirm 工具清单与 README 🔐 一致性；新工具走桥+确认
- [ ] 安全快照：workflows/Secrets 引用、Manifest exported/permission、Web 绑定、minify 状态
- [ ] 文档链接批量校验；N1/N3/N4 复跑；新 docs Draft 版本与 milestone 勾选
- [ ] 仓内 APK versionCode vs HEAD；Actions artifact 状态
- [ ] 密钥例行扫描 + origin/credential 卫生
- [ ] coroutines 解析版本（R4）；explain_text/archive/技能开关接线状态

## 附录 A：v1→v2 差异速查
- 代码：eec4139cd..53096e427 = 31 文件/+3006/-128（9 Java+2 配置+4 workflow+7 docs+2 strings+3 res+...）。ai/ 零差异。
- 修复：S1(入口)/S2、S3(绑定)、S7(legado.yml)、泄漏、预取可取消、Retrofit LRU、TLS 收紧、R8。
- 遗留开放：S3(token/CORS)/S4/S5/S6/S9/S10/S11(回归面)/S12(兼容面) + 文档 N1/N3。

## 附录 B：证据索引（v2 新增部分）

- 364e5cc65（security+R8，08-31 09:30）· 217bb355b/29d0fd799/c49091a2d（fix）· eb038b0d5（perf）· c4b8b0333..af3d151d3（web 打包）· 2b09e81ec/22d0c4633（backup）· e3ca24285..53096e427（6 docs）
- app/build.gradle:59（release minify true）· proguard-rules.pro +4（-keep io.legado.app.ai.**）
- AndroidManifest.xml:22-28（READER_DATA permission）/ :378-383（read/writePermission）
- .github/workflows/ai-build.yml:44-59（Secrets 注入 + unsigned 回退）· workflows 目录（3 条）
- app/src/main/java 增量 9 文件（见 §3.1 表）· PreferKey +allowUnsafeCertificate/webLanAccess
- docs 增量：docs/{ai,architecture×3,ui,performance} + specs/2026-09-05-project-improvement-plan.md
- v1 全量证据链见分支 docs/map-report-2026-09-baseline-eec4139cd 的报告（f926ffed8）

---

*v2 由主控代理在新基线 53096e427 上复核撰写；旧基线结论以 v1（存档分支）为准，本版标注「不变项」的均已重测确认。*
